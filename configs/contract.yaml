migration:
  name: "caso1-ecommerce"
  description: "Migración full de modelo e-commerce: customers, products, orders, order_items"
  version: "1.0"
  author: "${USERNAME}"

tables:
  customers:
    source:
      schema: "public"
      table: "customers"
    target:
      schema: "public"
      table: "customers"
    enabled: true
    migrationType: "full"

  products:
    source:
      schema: "public"
      table: "products"
    target:
      schema: "public"
      table: "products"
    enabled: true
    migrationType: "full"

  orders:
    source:
      schema: "public"
      table: "orders"
    target:
      schema: "public"
      table: "orders"
    enabled: true
    migrationType: "full"

  order_items:
    source:
      schema: "public"
      table: "order_items"
    target:
      schema: "public"
      table: "order_items"
    enabled: true
    migrationType: "full"
