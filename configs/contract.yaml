migration:
  name: "nombre_migracion"
  description: "descripcion_de_la_migracion"
  version: "version"
  author: "nombre_del_autor"

tables:
  nombre_tabla_1:
    source:
      schema: "nombre_esquema"
      table: "nombre_tabla"
    target:
      schema: "nombre_esquema"
      table: "nombre_tabla"
    enabled: true
    migrationType: "full"
    incrementalConfig:
      column: "nombre_columna"
      type: "tipo_de_columna"
      startValue: "valor_inicial"
      batchSize: 1000
    filters:
      where: "condicion SQL"
